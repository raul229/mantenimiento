# Desarrollo: deja DATABASE_URL vacío para usar SQLite (backend/db.sqlite3).
# Producción (Render + Supabase): pega la URL de Postgres.
SECRET_KEY=cambia-esta-clave-en-produccion
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Vacío = SQLite. En Render usa la URI de Supabase (pooler, puerto 6543).
DATABASE_URL=
# DB_SSL_REQUIRE=True
# DB_CONN_MAX_AGE=0

CORS_ALLOWED_ORIGINS=http://localhost:5174,http://127.0.0.1:5174
FRONTEND_ORIGIN=http://localhost:5174
